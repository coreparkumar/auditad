Implementation Prompt: Real Android Diagnostics Engine for AuditAdRole & ContextYou are an expert Android Native and Capacitor/TypeScript engineer. Your task is to implement a fully functional, real-time Diagnostic Center for AuditAd by replacing the current simulated DNS stream with a functional live Android-native diagnostics pipeline.This is a hybrid React + TypeScript + Vite + Capacitor app. The native Android source lives under android/app/src/main/java.Target Files to Modifysrc/App.tsx (State management, layout wiring)src/components/DNSStream.tsx (UI feed updates, event rendering)src/types.ts (Event schemas)android/app/src/main/java/com/auditad/app/AppCheckPlugin.java (Capacitor Bridge)android/app/src/main/AndroidManifest.xml (Permissions & Service declarations)capacitor.config.tsTechnical Architecture Requirements1. Android Native Layer (AppCheckPlugin.java)Implement or extend the Capacitor Plugin to expose methods: startDiagnostics(), stopDiagnostics(), and an asynchronous listener/event emitter onDiagnosticEvent.Implement a real Android mechanism to capture network/DNS telemetry (e.g., look into using a local loopback VpnService, Android NetworkCallback, or DnsResolver logs depending on the least-privileged path that yields domain telemetry).Handle foreground service lifecycles if persistent tracking is required, ensuring a persistent notification is shown to prevent OS termination.Categorize domains/events natively or pass them to the web layer with package names, timestamps, domains, and latencies.2. Capacitor Bridge & TypeScript Layer (src/types.ts)Standardize the DiagnosticEvent interface:interface DiagnosticEvent {
  id: string;
  timestamp: number;
  packageName: string;
  domain: string;
  category: 'Ads' | 'Analytics' | 'Trackers' | 'Allowed' | 'Unknown';
  status: 'blocked' | 'allowed';
  latencyMs: number;
}
Map engine operational states explicitly: Idle, Starting, Active, Error, Unsupported.3. Frontend UI Layer (src/components/DNSStream.tsx & src/App.tsx)Completely remove the setInterval or random mock generator currently driving the stream.Wire AppCheckPlugin.addListener('onDiagnosticEvent', ...) directly to the React state.Ensure the live counters (Ads blocked, trackers allowed, etc.), filters, and search bar aggregate data dynamically from this real state history.Step-by-Step Execution Plan for AgentPlease execute the following steps sequentially. Do not move to the next step until the previous one is fully implemented:Step 1: Audit & Clean CodebasesScan src/components/DNSStream.tsx and src/App.tsx. Identify and strip out all mock data arrays, random domain lists, and simulated ticking timers.Step 2: Native Android Manifest & Plugin SetupUpdate AndroidManifest.xml to declare necessary permissions (e.g., INTERNET, ACCESS_NETWORK_STATE, or BIND_VPN_SERVICE if compiling a local interceptor). Update AppCheckPlugin.java to define the plugin methods and set up the event bridge notification system via notifyListeners().Step 3: Implement Live Data PipelineBuild out the concrete Android telemetry logic in Java. Ensure it safely handles exceptions, checks if the device supports the required APIs, and catches runtime permission denials, emitting an error state back to the web UI instead of crashing.Step 4: React UI State WiringConnect the Capacitor listeners inside a useEffect hook within the React application. Map incoming native telemetry payloads to the UI counters and feed history. Ensure performance optimization (e.g., virtualized lists or maximum log counts) so the UI doesn't lag during high-frequency events.Verification & Acceptance CriteriaNo Mock Data: Code inspection proves that zero random/hardcoded data generators remain in the active code path.State Transparency: The UI clearly transitions through Idle -> Starting -> Active (or Error/Unsupported if permissions are denied or running on an incompatible emulator environment).Responsive UI: The event feed handles real-time data streaming without blocking the main browser thread.